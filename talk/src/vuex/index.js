import Vue from "vue"
import Vuex from "vuex"

Vue.use(Vuex)

const state = {
  
}
const mutations = {
 
}
const action = {


}
const getters = {
	
}
export default new Vuex.Store({
  state,
  mutations,
  action,
  getters,

})
